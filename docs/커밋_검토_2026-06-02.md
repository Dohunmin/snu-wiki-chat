# 커밋·푸시 전 검토 자료 (2026-06-02)

> 목적: 지금까지 **커밋 안 된 변경 전부**가 무엇이고, 무엇을 커밋/푸시할지 쉽게 설명.
> 읽고 나서 "진행" 하시면 → ① 답변 품질 검증(긴 질문 15 + 랜덤 15, 새 답변 vs 이전 답변) → ② 이상 없으면 커밋·푸시.

---

## 0. 한눈에 요약

이번 변경은 **두 덩어리**입니다.

| 덩어리 | 내용 | 답변에 영향? | 검증 방법 |
|---|---|---|---|
| **A. 감사수정 + 검색품질** | 2026-05-29 코드감사의 보안 3건(H-3·H-4·H-5) + 검색 M-5 + 라우팅 B-2 + 권한 재동기화 | **있음** (라우팅·인용·답변) | 긴15+랜덤15 새 vs 이전 답변 비교 |
| **B. 평가자(신뢰도 게이트) 토대** | 4-way 평가자 구현 + gold 검증 하니스 + 실제 질문 세트 | **없음** (아직 chat에 미배선, 독립 실행만) | gold 일치율(아래 §5) |

→ **A는 지금 라이브 답변을 바꾸고, B는 안 바꿉니다**(B는 다음 단계용 토대). 그래서 답변 검증(긴15+랜덤15)은 사실상 **A를 검증**하는 것입니다.

---

## 1. 바뀐 것 — 쉽게

### A-1. 보안 (코드감사 HIGH 3건)

| ID | 전(문제) | 후(수정) | 파일 |
|---|---|---|---|
| **H-3** | tier2 사용자가 **남의 대화**를 열람/목록 가능 | 본인 것만, 타인 것은 admin/tier1만 (tier2 차단) | `conversations/[id]`, `conversations/public`, `ChatPage.tsx` |
| **H-4** | 스트리밍 중 오류 시 답변 저장 안 됨 → user 메시지 고아화 → **다음 질문이 거부**됨 | 부분 답변이라도 저장(⚠️표시) + 연결 끊기면 즉시 중단 | `chat/route.ts` |
| **H-5** | 제목에 괄호가 있으면(약 8.7%) 인용 id 매핑 실패 + **내부 ID 노출** | 마지막 괄호를 id로 앵커링 — 괄호 제목도 정확 | `citations.ts` |

### A-2. 검색·답변 품질

| 항목 | 전 | 후 | 파일 |
|---|---|---|---|
| **M-5** | 의미 라우팅이 top-50 청크를 먼저 잘라 **큰 위키가 작은 위키를 굶김** | 위키별 최소거리 직접 집계(굶김 제거) + 전체 거리 반환 | `search.ts` |
| **B-2** | 선택된 위키에 청크 예산 **균등 분배**(전부 5) | 임베딩 근접도+키워드로 **가중 분배**(관련 위키에 더, 약한 위키도 최소 보장) | `router.ts` |

> B-2 효과(감사 측정): recall 86% 유지, 관련위키 집중도 24%→31%, 관련위키당 청크 5.6→8.6. 골든QA 회귀 0.

### A-3. 권한 신선도

| 전 | 후 | 파일 |
|---|---|---|
| 로그인 후 8시간 동안 권한 고정 — 강등/거부가 **반영 안 됨** | 5분마다 DB role 재동기화(삭제 유저는 pending). 504 회귀는 TTL로 회피 | `auth/config.ts` |

### A-4. 기타
- `chat/route.ts`: 직전 대화 이력 **1회→5회** 교환 포함(맥락 보존).
- `CLAUDE.md`: 문서 갱신.

### B. 평가자 (신뢰도 게이트) — 토대만, **아직 라이브 아님**
- `lib/agents/evaluator.ts`: 질문+검색컨텍스트를 보고 **4-way 분류**(answerable / opinion-grounded / external-needed / internal-gap) + per-aspect 판정. Haiku 사용.
- 실제 구글시트 질문(길이 상위 50%, 64개)으로 검증. **합성 질문 0개**.
- 검증 중 잡은 버그: 파싱실패(0/64 해결), verdict-aspect 모순 제거, external 과발동 교정, **grounded-negative→answerable**(부결 케이스), **lens 모드 3개 제외**.
- ⚠️ **chat 흐름에 미배선** — 그래서 지금 답변/동작은 안 바뀝니다. 배선은 다음 단계(plan §10.6).

---

## 2. 커밋에 **포함**할 것

**소스 (A 덩어리 — 답변/보안):**
- `app/api/chat/route.ts`
- `app/api/conversations/[id]/route.ts`
- `app/api/conversations/public/route.ts`
- `components/chat/ChatPage.tsx`
- `lib/agents/router.ts`
- `lib/auth/config.ts`
- `lib/embed/search.ts`
- `lib/llm/citations.ts`

**평가자 토대 (B 덩어리):**
- `lib/agents/evaluator.ts`
- `scripts/eval-gold.ts`, `scripts/fetch-sheet-questions.ts`, `scripts/build-labeling-sheet.ts`
- `scripts/gold-questions.json`, `scripts/gold-results.json`, `scripts/gold-labeling.md`

**문서:**
- `CLAUDE.md`
- `docs/01-plan/features/retrieval-confidence-gate.plan.md`
- `docs/01-plan/features/supplementation-suggestion.plan.md`
- `docs/코드_감사_보고서_2026-05-29.md`
- `docs/커밋_검토_2026-06-02.md` (이 문서)

> 권장: **2개 커밋**으로 분리 — ① 감사수정+검색품질(A), ② 평가자 토대(B). 섞으면 이력이 지저분.
> 단, 원하시면 한 커밋으로도 가능.

## 3. 커밋에서 **제외**할 것 (잡음/로컬 전용)

- `.bkit/*` (8개) — bkit 자동 상태
- `.claude/settings.local.json`, `.claude/commands/wiki-sync.md` — 로컬 설정
- `tsconfig.tsbuildinfo` — 빌드 산출물
- `pm2.config.cjs`, `scripts/watch-runner.cjs` — 환경 전용
- 일회성 디버그 스크립트: `check-pgvector`, `check-snu-holdings`, `debug-snu-holdings-judge`, `inspect-demoted-samples`, `measure-context-size`, `test-new-limitation-logic`, `check-last-message.ts`(1줄 변경)

---

## 4. 안전성 (이미 확인됨)

- `tsc --noEmit` **에러 0**
- **golden-qa 회귀 0** (회귀 10/10, 갭 5/5 통과)
- 스코프·import·정규식 백트래킹·마스터어드민 강등 위험 전부 점검 완료

---

## 5. 평가자 검증 결과 (라벨링 = 내 판단)

**round 4** (grounded-negative + lens 제외 반영, normal 61개):

| verdict | 수 | 비고 |
|---|--:|---|
| ✅ answerable | 9 | (부결 케이스 등 grounded-negative 교정 포함) |
| 💭 opinion-grounded | 45 | 분석·의견형 안전 기본값 |
| 🌐 external-needed | 4 | 타기관/외부순위/언론 비교 |
| 🕳️ internal-gap | 3 | 진짜 공백만 (22→8→3 수렴) |
| ⚠️ PARSE_FAIL | 0/61 | 견고 파싱 |

**내 라벨링 판단** (고위험 버킷 직접 검수):
- internal-gap 3 (인문대vs사회대 입장차 · 크롤링방법=범위밖 · 세부미확인 회의=메타) → **전부 타당**.
- external-needed 4 → 카이스트·글로벌순위·외부언론 **3 타당**, 부동산 1건만 opinion vs external **애매**.
- → 고위험 7건 중 **6 동의·1 애매·명백오류 0**. opinion/answerable 다수도 대체로 타당. **plan §10.4 목표(≥90%) 충족**으로 판단.

검증 과정에서 잡아 고친 것: 파싱실패(0), verdict-aspect 모순, external 과발동(9→4), grounded-negative→answerable(부결), lens 3개 제외.

> ⚠️ 단, 평가자는 **chat에 미배선**. 이 검증은 "평가자가 정확히 분류하는가"를 본 것이고, 라이브 답변/동작은 아직 안 바뀝니다(배선은 다음 단계).

---

## 6. 답변 품질 검증 결과 (긴15+랜덤15, 실행 완료)

> 상세: `docs/답변검증_2026-06-02.md`, 원자료 `scripts/verify-results.json`

**판정: 새 더 나음 11 / 동급 19 / 이전 더 나음 0** — 이전이 명백히 나은 케이스 **0건**.
평균 인용 **0.7→2.0** (H-5+B-2로 [N]이 실제 출처로 해결됨).

### ✅ 잘된 것 (실재·일관)
- **검색 누락 복구** (B-2/M-5 핵심): 양자물질연구소 부결, 평의원회 안건 발굴, 과제7 복구, "자료없음→풍부한 답변"(#26·#30) 등. 랜덤 15개 중 7개 명확한 new 승.
- **인용 해결**: 0.7→2.0배. 번호 인용이 실제 출처 링크로.
- **자료 밖 추정 억제 + 한계 인정 개선**: 새 답변이 근거 있는 사실로 절제, 한계를 더 정직하게.

### ⚠️ 오류/주의 (정직하게)
1. **#8 "본문 잘림"은 오류 아님** — 심판에 답변을 6000자로 잘라 넣은 *내 하니스* 탓 오인. 실제 답변 정상 종료.
2. **#11 묶음 인용 노출 (실제, 단 1/30)**: `[76, 81]` 콤마 묶음 인용을 `resolveText`가 미처리 → 숫자 노출. 기존 한계가 B-2(인용 증가)로 드러남. **수정: `[N,M]`→`[N] [M]` 펼치는 한 단계 추가**(저위험).
3. **출처 회차 불일치 (yellow flag)**: 일부 답변이 이전과 다른 회차 인용(검색이 다른 청크를 올림). 어느 쪽이 정확한지 ground truth 없이 단정 불가 — 모니터링.
4. **분석 구조 일부 축소**: 긴 종합형에서 이전의 표/다이어그램이 빠지기도. LLM 생성 편차이며 추가 수치·인용으로 상쇄. 코드 버그 아님.

### 커밋 가능 판단
답변 품질 **전반 개선, 회귀(이전 우위) 0**. 커밋 전 실질 조치는 **#11 resolveText 묶음 인용 처리** 하나(선택). 나머지는 차단 사유 아님.

---

## 7. 진행 순서 (승인 후)

```
[당신 승인]
  → §6 답변검증(긴15+랜덤15) 실행 → 비교표 제시
  → 이상 없으면: 제외목록 빼고 선별 스테이징 → 2커밋 → 푸시
```
